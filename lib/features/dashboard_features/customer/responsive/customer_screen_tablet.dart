import 'package:flutter/material.dart';

class CustomerScreenTablet extends StatelessWidget {
  const CustomerScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Customer Screen')));
  }
}
