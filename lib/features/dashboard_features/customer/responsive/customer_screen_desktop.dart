import 'package:flutter/material.dart';

class CustomerScreenDesktop extends StatelessWidget {
  const CustomerScreenDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Customer Screen')));
  }
}
