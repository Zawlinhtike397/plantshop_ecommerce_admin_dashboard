import 'package:flutter/material.dart';

class DiscountScreenTablet extends StatelessWidget {
  const DiscountScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Discount Screen')));
  }
}
